// Done by JS Interpreter Automatically
let str = "5";
let num = 5;
let num2 = 34;

// + is use to concat string so number convert in string
console.log(str + num);
// First two oprends are number so addition then after concat and convert in string
console.log(num + num2 + str);
// First oprand are string so all are convert to string and concat
console.log(str + num + num2);
// string convert in number
console.log(num2 - str);
