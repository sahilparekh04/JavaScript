// Selecting elements by id
let headingById = document.getElementById("first-heading");
let paraById = document.getElementById("first-para");

/*
    innerText = shows visible text contained in node exectly which shows in screen

    innerContent = shows hidden text and show case value or text as our text editor takes content from html file.

    innerHTML = show case text with HTML mark up tags also

*/

// headingById.innerText = "Hello world in JS.";

// headingById.innerHTML = "<u>Hello world in JS.</u>";

// show case hidden text and as it is like text editor
// headingById.textContent;
