let numArr = [10, 20, 30];
let nameArr = ["Sahil", "Vivek", "Kenil"];

// merge or concat two arrays but which array you perform action that returns first
console.log(numArr.concat(nameArr));
console.log(numArr);
console.log(nameArr);

// Storing in diffrent variable
let newArr = numArr.concat(nameArr);
console.log(newArr);
